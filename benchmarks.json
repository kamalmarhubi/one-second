[
    {
        "code": "#!/usr/bin/env python\n\nimport json\n\n#j = json.dumps({'%d' % (i): i for i in xrange(10000)})\nj = json.dumps(range(10000))\n\ndef f(n):\n    for _ in xrange(n):\n        json.loads(j)\n\nimport sys\nf(int(sys.argv[1]))\n",
        "exact_iters": 798,
        "rounded_iters": 1000,
        "source": "benchmarks/json_parse.py"
    },
    {
        "code": "#!/usr/bin/env python\n\ndef f(n):\n    for _ in xrange(n):\n        pass\n\nimport sys\nf(int(sys.argv[1]))\n",
        "exact_iters": 67618085,
        "rounded_iters": 100000000,
        "source": "benchmarks/loop.py"
    },
    {
        "code": "#!/usr/bin/env python\n\nimport json\n\ndef f(n):\n    for _ in xrange(n):\n        []\n\nimport sys\nf(int(sys.argv[1]))\n",
        "exact_iters": 31544333,
        "rounded_iters": 10000000,
        "source": "benchmarks/make_empty_list.py"
    },
    {
        "code": "#!/usr/bin/env python\n\nimport json\n\ndef f(n):\n    for _ in xrange(n):\n        [0,1,2,3]\n\nimport sys\nf(int(sys.argv[1]))\n",
        "exact_iters": 10050992,
        "rounded_iters": 10000000,
        "source": "benchmarks/make_four_elt_list.py"
    },
    {
        "code": "#!/usr/bin/env python\n\nimport json\n\ndef f(n):\n    for i in xrange(n):\n        [0]\n\nimport sys\nf(int(sys.argv[1]))\n",
        "exact_iters": 12161702,
        "rounded_iters": 10000000,
        "source": "benchmarks/make_one_elt_list.py"
    },
    {
        "code": "#!/bin/bash\n\nfor i in $(seq $1); do\n    /usr/bin/env true;\ndone\n",
        "exact_iters": 305,
        "rounded_iters": 100,
        "source": "benchmarks/run_bin_true.sh"
    },
    {
        "code": "#!/bin/bash\n\nfor i in $(seq $1); do\n    python -c '';\ndone\n",
        "exact_iters": 63,
        "rounded_iters": 100,
        "source": "benchmarks/run_python.sh"
    },
    {
        "code": "#include <stdlib.h>\n\nint main(int argc, char **argv) {\n    int n, i, s;\n    n = atoi(argv[1]);\n\n    for (s = i = 0; i < n; ++i) {\n        s += 1;\n    }\n\n    return 0;\n}\n",
        "exact_iters": 550429840,
        "rounded_iters": 1000000000,
        "source": "benchmarks/sum.c"
    },
    {
        "code": "#!/usr/bin/env python\n\ndef f(n):\n    s = 0\n    for i in xrange(n):\n        s += 1\n\nimport sys\nf(int(sys.argv[1]))\n",
        "exact_iters": 26069696,
        "rounded_iters": 10000000,
        "source": "benchmarks/sum.py"
    }
]
