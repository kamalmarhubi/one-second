{"json_parse.py": {"rounded_iters": 1000, "code": "#!/usr/bin/env python\n\nimport json\n\n#j = json.dumps({'%d' % (i): i for i in xrange(10000)})\nj = json.dumps(range(10000))\n\ndef f(n):\n    for _ in xrange(n):\n        json.loads(j)\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 1063}, "write_to_memory.py": {"rounded_iters": 1000000000, "code": "#!/usr/bin/env python\n\nimport cStringIO\n\nLENGTH = 1000000\ns = \"a\" * LENGTH\n\ndef f(n):\n    output = cStringIO.StringIO()\n    bytes_written = 0\n    while bytes_written < n:\n        output.write(s)\n        bytes_written += LENGTH\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 1900233105}, "make_one_elt_list.py": {"rounded_iters": 10000000, "code": "#!/usr/bin/env python\n\nimport json\n\ndef f(n):\n    for i in xrange(n):\n        [0]\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 12161702}, "make_empty_list.py": {"rounded_iters": 10000000, "code": "#!/usr/bin/env python\n\nimport json\n\ndef f(n):\n    for _ in xrange(n):\n        []\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 28676666}, "make_four_elt_list.py": {"rounded_iters": 10000000, "code": "#!/usr/bin/env python\n\nimport json\n\ndef f(n):\n    for _ in xrange(n):\n        [0,1,2,3]\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 6240873}, "write_to_disk.py": {"rounded_iters": 1000000000, "code": "#!/usr/bin/env python\n\nimport tempfile\nimport os\n\nLENGTH = 1000000\ns = \"a\" * LENGTH\n\ndef cleanup(f, name):\n    f.flush()\n    os.fsync(f.fileno())\n    f.close()\n    try:\n        os.remove(name)\n    except:\n        pass\n\ndef f(n):\n    name = './out'\n    f = open(name, 'w')\n    bytes_written = 0\n    while bytes_written < n:\n        f.write(s)\n        bytes_written += LENGTH\n    cleanup(f, name)\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 805884330}, "sum.py": {"rounded_iters": 10000000, "code": "#!/usr/bin/env python\n\ndef f(n):\n    s = 0\n    for i in xrange(n):\n        s += 1\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 26069696}, "loop.py": {"rounded_iters": 100000000, "code": "#!/usr/bin/env python\n\ndef f(n):\n    for _ in xrange(n):\n        pass\n\nimport sys\nf(int(sys.argv[1]))\n", "exact_iters": 67618085}, "run_python.sh": {"rounded_iters": 100, "code": "#!/bin/bash\n\nfor i in $(seq $1); do\n    python -c '';\ndone\n", "exact_iters": 51}, "run_bin_true.sh": {"rounded_iters": 100, "code": "#!/bin/bash\n\nfor i in $(seq $1); do\n    /usr/bin/env true;\ndone\n", "exact_iters": 305}, "sum.c": {"rounded_iters": 1000000000, "code": "#include <stdlib.h>\n\nint main(int argc, char **argv) {\n    int n, i, s;\n    n = atoi(argv[1]);\n\n    for (s = i = 0; i < n; ++i) {\n        s += 1;\n    }\n\n    return 0;\n}\n", "exact_iters": 605472824}}